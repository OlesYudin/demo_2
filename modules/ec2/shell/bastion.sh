#!/bin/bash

sudo apt update && sudo apt -y upgrade

# sudo apt update && sudo apt -y upgrade
# # Install docker
# sudo apt install -y docker.io
# # Execute container
# sudo docker pull olesyudin/password-generator:53
# sudo docker run -d -p 80:80 --name=password-generator olesyudin/password-generator:latest